# Copy to .env and adjust for the target host.
OPENMETADATA_VERSION=1.12.6
OPENMETADATA_PORT=8585
OPENMETADATA_ADMIN_PORT=8586
INGESTION_PORT=8080
OPENMETADATA_BIND_ADDRESS=127.0.0.1

OPENMETADATA_CLUSTER_NAME=openmetadata
LOG_LEVEL=INFO
OPENMETADATA_HEAP_OPTS=-Xmx1G -Xms1G

AUTHENTICATION_PROVIDER=basic
AUTHENTICATION_ENABLE_SELF_SIGNUP=false
# Required: generate a unique valid Fernet key before starting.
# Example: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=
AUTHENTICATION_PUBLIC_KEYS=[http://localhost:8585/api/v1/system/config/jwks]

ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_SCHEME=http
SEARCH_TYPE=elasticsearch

PIPELINE_SERVICE_CLIENT_ENABLED=true
PIPELINE_SERVICE_CLIENT_ENDPOINT=http://ingestion:8080
SERVER_HOST_API_URL=http://openmetadata-server:8585/api
SECRET_MANAGER=db
